/** Adapted from Skiper UI skiper87: https://skiper-ui.com/v1/skiper87 */
export function ScrollFade({ children, className = '' }) {
  return <div className={`scroll-fade-y ${className}`}>{children}</div>
}
